<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <h2><%= subtitle %></h2>
    <p>Agora: <%= date %></p>

    <section>
      <h3>Lista de nomes</h3>
      <ul>
        <% for(var i=0; i < list.length; i++) { %>
          <li><%= list[i] %></li>
       <% } %>
      </ul>

    </section>
  </body>
</html>
